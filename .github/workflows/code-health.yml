name: Code Health

on:
  push:
    branches:
      - Local
    paths:
      - "EngineData/Frontend/RustApp/src/**"
      - "EngineData/Frontend/RustApp/src-tauri/src/**"
      - "EngineData/Frontend/RustApp/scripts/**"
      - "EngineData/Frontend/RustApp/package.json"
      - "EngineData/Frontend/RustApp/package-lock.json"
      - "EngineData/Frontend/RustApp/tsconfig.json"
      - "EngineData/Frontend/RustApp/src-tauri/Cargo.toml"
      - "EngineData/Frontend/RustApp/src-tauri/Cargo.lock"
      - "EngineData/Backend/LocalWorker/WorkerRuntime/**/*.py"
      - "EngineData/Backend/LocalWorker/WorkerRuntime/pyproject.toml"
      - "EngineData/Backend/LocalWorker/WorkerRuntime/uv.lock"
      - ".github/workflows/code-health.yml"
  pull_request:
    branches:
      - Local
    paths:
      - "EngineData/Frontend/RustApp/src/**"
      - "EngineData/Frontend/RustApp/src-tauri/src/**"
      - "EngineData/Frontend/RustApp/scripts/**"
      - "EngineData/Frontend/RustApp/package.json"
      - "EngineData/Frontend/RustApp/package-lock.json"
      - "EngineData/Frontend/RustApp/tsconfig.json"
      - "EngineData/Frontend/RustApp/src-tauri/Cargo.toml"
      - "EngineData/Frontend/RustApp/src-tauri/Cargo.lock"
      - "EngineData/Backend/LocalWorker/WorkerRuntime/**/*.py"
      - "EngineData/Backend/LocalWorker/WorkerRuntime/pyproject.toml"
      - "EngineData/Backend/LocalWorker/WorkerRuntime/uv.lock"
      - ".github/workflows/code-health.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    name: Detect changed source domains
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      frontend: ${{ steps.scope.outputs.frontend }}
      python: ${{ steps.scope.outputs.python }}
      rust: ${{ steps.scope.outputs.rust }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
          fetch-depth: 0
      - id: scope
        name: Classify changed paths
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PUSH_BASE: ${{ github.event.before }}
          PR_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail

          frontend=false
          python=false
          rust=false
          base="$PUSH_BASE"
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            base="$PR_BASE"
          fi

          if [[ -z "$base" || "$base" =~ ^0+$ ]] || ! git cat-file -e "${base}^{commit}" 2>/dev/null; then
            frontend=true
            python=true
            rust=true
          else
            git diff --name-only "$base" "$GITHUB_SHA" > "$RUNNER_TEMP/translateit-changed-paths.txt"
            while IFS= read -r path; do
              case "$path" in
                .github/workflows/code-health.yml)
                  frontend=true
                  python=true
                  rust=true
                  ;;
                EngineData/Frontend/RustApp/src-tauri/src/*|EngineData/Frontend/RustApp/src-tauri/Cargo.toml|EngineData/Frontend/RustApp/src-tauri/Cargo.lock)
                  frontend=true
                  rust=true
                  ;;
                EngineData/Frontend/RustApp/src/*|EngineData/Frontend/RustApp/scripts/*|EngineData/Frontend/RustApp/package.json|EngineData/Frontend/RustApp/package-lock.json|EngineData/Frontend/RustApp/tsconfig.json)
                  frontend=true
                  ;;
                EngineData/Backend/LocalWorker/WorkerRuntime/*)
                  python=true
                  ;;
              esac
            done < "$RUNNER_TEMP/translateit-changed-paths.txt"
          fi

          echo "frontend=$frontend" >> "$GITHUB_OUTPUT"
          echo "python=$python" >> "$GITHUB_OUTPUT"
          echo "rust=$rust" >> "$GITHUB_OUTPUT"

  frontend:
    name: Frontend source health
    needs: changes
    if: needs.changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: EngineData/Frontend/RustApp
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: EngineData/Frontend/RustApp/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build:frontend
      - run: npm run test:frontend-runtime
      - run: npm run validate:source-size
      - run: npm run validate:bridge-types
      - run: npm run validate:command-parity
      - run: npm run validate:virtual-route
      - name: Audit production npm dependencies
        run: npm audit --omit=dev --audit-level=high

  python:
    name: Python source and unit health
    needs: changes
    if: needs.changes.outputs.python == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: EngineData/Backend/LocalWorker/WorkerRuntime
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12.10"
      - name: Install lock-resolved test tools only
        shell: bash
        run: |
          python - <<'PY' > "$RUNNER_TEMP/translateit-ci-dev-requirements.txt"
          import tomllib
          from pathlib import Path

          wanted = {"ruff", "pytest", "huggingface-hub", "ffmpeg-python", "numpy"}
          lock = tomllib.loads(Path("uv.lock").read_text(encoding="utf-8"))
          versions = {
              package.get("name"): package.get("version")
              for package in lock.get("package", [])
              if package.get("name") in wanted
          }
          missing = sorted(wanted - versions.keys())
          if missing:
              raise SystemExit(f"canonical uv.lock is missing CI test tools: {missing}")
          for name in sorted(wanted):
              print(f"{name}=={versions[name]}")
          PY
          python -m pip install -r "$RUNNER_TEMP/translateit-ci-dev-requirements.txt"
      - run: python -m compileall -q .
      - name: Python static correctness gate
        run: ruff check . --select E4,E7,E9,F,I
      - name: Python formatting gate
        run: ruff format --check .
      - name: Run Python contract tests
        run: python -m pytest -q

  python-windows:
    name: Windows Python source and unit health
    needs: changes
    if: needs.changes.outputs.python == 'true'
    runs-on: windows-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: EngineData/Backend/LocalWorker/WorkerRuntime
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12.10"
      - name: Install lock-resolved test tools only
        shell: bash
        run: |
          python - <<'PY'
          import subprocess
          import sys
          import tomllib
          from pathlib import Path

          wanted = {"ruff", "pytest", "huggingface-hub", "ffmpeg-python", "numpy"}
          lock = tomllib.loads(Path("uv.lock").read_text(encoding="utf-8"))
          versions = {
              package.get("name"): package.get("version")
              for package in lock.get("package", [])
              if package.get("name") in wanted
          }
          missing = sorted(wanted - versions.keys())
          if missing:
              raise SystemExit(f"canonical uv.lock is missing CI test tools: {missing}")
          requirements = [f"{name}=={versions[name]}" for name in sorted(wanted)]
          subprocess.check_call([sys.executable, "-m", "pip", "install", *requirements])
          PY
      - run: python -m compileall -q .
      - name: Windows Python static correctness gate
        run: ruff check . --select E4,E7,E9,F,I
      - name: Windows Python formatting gate
        run: ruff format --check .
      - name: Run Windows Python contract tests
        run: python -m pytest -q

  rust:
    name: Rust source and unit health
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: EngineData/Frontend/RustApp/src-tauri
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: EngineData/Frontend/RustApp/package-lock.json
      - name: Install frontend dependencies
        working-directory: EngineData/Frontend/RustApp
        run: npm ci
      - name: Build frontend
        working-directory: EngineData/Frontend/RustApp
        run: npm run build:frontend
      - name: Install Linux Tauri dependencies
        run: >-
          sudo apt-get update && sudo apt-get install -y
          libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev
          librsvg2-dev libasound2-dev patchelf
      - name: Rust compiler/dead-code gate
        env:
          RUSTFLAGS: "-Ddead_code -Dunused_imports -Dunused_variables -Dunused_mut -Dunused_assignments"
        run: cargo check --locked
      - name: Rust Clippy correctness gate
        run: cargo clippy --locked --all-targets -- -D clippy::correctness
      - name: Run Rust unit tests
        run: cargo test --locked -- --test-threads=1

  rust-windows:
    name: Windows Rust source and unit health
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: windows-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: EngineData/Frontend/RustApp/src-tauri
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: EngineData/Frontend/RustApp/package-lock.json
      - name: Install frontend dependencies
        working-directory: EngineData/Frontend/RustApp
        run: npm ci
      - name: Build frontend
        working-directory: EngineData/Frontend/RustApp
        run: npm run build:frontend
      - name: Show hosted Windows Rust toolchain
        run: |
          rustc --version
          cargo --version
      - name: Windows Rust compiler gate
        env:
          RUSTFLAGS: "-Ddead_code -Dunused_imports -Dunused_variables -Dunused_mut -Dunused_assignments"
        run: cargo check --locked
      - name: Windows Rust Clippy correctness gate
        run: cargo clippy --locked --all-targets -- -D clippy::correctness
      - name: Run Windows Rust unit tests
        run: cargo test --locked -- --test-threads=1
