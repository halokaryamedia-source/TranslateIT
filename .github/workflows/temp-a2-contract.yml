name: Retired Temporary Contract Proof

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: windows-2022
    steps:
      - name: Retired
        shell: pwsh
        run: Write-Host 'This temporary proof workflow is retired.'
