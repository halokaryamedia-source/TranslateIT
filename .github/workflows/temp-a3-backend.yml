name: Retired Temporary A3 Proof

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: windows-2022
    steps:
      - name: Retired
        shell: pwsh
        run: Write-Host 'A3 proof is closed; this workflow is retained only because deletion was blocked by connector policy.'
