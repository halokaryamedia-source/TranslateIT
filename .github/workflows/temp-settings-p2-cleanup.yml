name: Temporary Settings P2 Cleanup

on:
  push:
    branches: [New]
    paths:
      - .github/workflows/temp-settings-p2-cleanup.yml

permissions:
  contents: write

jobs:
  cleanup-and-prove:
    runs-on: windows-2022
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: New
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: EngineData/Frontend/RustApp/package-lock.json
      - name: Apply bounded Settings P2 cleanup
        shell: pwsh
        run: |
          python .github/scripts/temp-settings-p2-cleanup.py
          git diff --check
      - name: Verify bounded frontend ownership
        shell: pwsh
        run: |
          $changed = git diff --name-only
          $allowed = @('EngineData/Frontend/RustApp/src/pages/Settings.svelte')
          foreach ($path in $changed) {
            if ($allowed -notcontains $path) { throw "Unexpected product-source change: $path" }
          }
      - name: Install frontend dependencies
        working-directory: EngineData/Frontend/RustApp
        run: npm ci
      - name: Frontend typecheck
        working-directory: EngineData/Frontend/RustApp
        run: npm run typecheck
      - name: Frontend production build
        working-directory: EngineData/Frontend/RustApp
        run: npm run build:frontend
      - name: Commit Settings P2 cleanup
        shell: pwsh
        run: |
          git config user.name 'halokaryamedia-source'
          git config user.email 'halokaryamedia@gmail.com'
          git add EngineData/Frontend/RustApp/src/pages/Settings.svelte
          git commit -m 'Simplify Settings meeting audio UX'
          git push origin HEAD:New
