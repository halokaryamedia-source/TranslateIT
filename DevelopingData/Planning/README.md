# Historical Planning

This directory preserves earlier planning artifacts such as the inherited V1 architecture plan.

These files are historical evidence, not the active plan or current development queue. The single active continuation owner is:

```text
docs/knowledge/next-action.md
```

Do not create new per-task plans, progress notes, or current architecture authority in this directory. Current durable product/system policy belongs in `docs/foundation/`; durable decision reasoning belongs in `docs/knowledge/decision-log.md`.
