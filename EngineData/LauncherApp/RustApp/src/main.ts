import { invoke } from "@tauri-apps/api/core";
import "./styles.css";

type CommandResult = { ok: boolean; state: string; message: string };
type EngineStatus = { app_version: string; runtime_stage: string; lifecycle_state: string; cuda_policy: string; asr_engine: string; translation_engine: string; tts_engine: string; notes: string[] };
type RuntimeDiagnostics = { cuda_probe: { nvidia_smi_available: boolean; gpu_summary: string | null; cuda_runtime_ready: boolean; blocker: string | null }; backend_validation: { ready: boolean; blocker: string }; blockers: string[] };
type RuntimeStatusBundleReport = { engine_status: EngineStatus; readiness: { ready_for_user_facing_runtime: boolean; blockers: string[]; note: string }; capture_gate: { ready_for_capture_start: boolean; blockers: string[]; note: string }; live_capture: { stream_active: boolean; frames_received: number; device_name: string | null; blocker: string; note: string }; local_worker_manifest?: { ok: boolean; asr_model_ready: boolean; realtime_translation_model_ready: boolean; quality_translation_model_ready: boolean; piper_ready: boolean; blockers: string[]; note: string }; internal_validation_gate?: { progress_percent: number; blockers: string[]; note: string; local_worker_manifest?: RuntimeStatusBundleReport["local_worker_manifest"] }; live_pipeline_gate?: { progress_percent: number; blocker: string; note: string }; next_action: string; summary: string };
type RuntimeSettings = { schema_version: number; language_focus_mode: string; runtime_profile: string; source_language: string; target_language: string; audio: { input_device_id: string | null; output_device_id: string | null; sensitivity: number; input_sensitivity: string; show_advanced_devices: boolean; allow_low_but_usable_input: boolean; allow_cpu_degraded_mode: boolean; auto_play_translation_voice: boolean; auto_play_out_voice: boolean; use_custom_voice_actor: boolean; voice_actor_profiles_root: string }; voice_actor_profile_id: string };
type InputPreparationStatus = { ready: boolean; selected_device_name?: string | null; device_count?: number; blocker?: string; note?: string };
type LauncherChatSession = { session_id: string; title: string; kind: string; created_unix_ms: number; updated_unix_ms: number; messages: { role: string; content: string; created_unix_ms: number }[] };
type LauncherChatSummary = { session_id: string; title: string; kind: string; updated_unix_ms: number; message_count: number };
type LauncherChatActionResult = { ok: boolean; session_id: string; message: string };
type HardwareMetric = { label: string; percent: number | null; status: string; detail: string };
type HardwareUsageReport = { cpu: HardwareMetric; ram: HardwareMetric; gpu: HardwareMetric; note: string };
type SettingsTab = "general" | "audio" | "translate" | "developer";
type ChatKind = "recent" | "unsaved" | "saved" | "local";

const app = document.querySelector<HTMLDivElement>("#app");
if (!app) throw new Error("TranslateIT app root was not found.");

const icons = { plus: `<svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>`, clock: `<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="8"/><path d="M12 8v4l3 2"/></svg>`, file: `<svg viewBox="0 0 24 24"><path d="M7 3h7l4 4v14H7z"/><path d="M14 3v5h5"/></svg>`, folder: `<svg viewBox="0 0 24 24"><path d="M3 7h7l2 2h9v9H3z"/></svg>`, shield: `<svg viewBox="0 0 24 24"><path d="M12 3l7 3v5c0 5-3.2 8.3-7 10-3.8-1.7-7-5-7-10V6z"/></svg>`, chevron: `<svg viewBox="0 0 24 24"><path d="M8 10l4 4 4-4"/></svg>`, mic: `<svg viewBox="0 0 24 24"><rect x="9" y="3" width="6" height="11" rx="3"/><path d="M5 11a7 7 0 0 0 14 0M12 18v3M9 21h6"/></svg>`, micOff: `<svg viewBox="0 0 24 24"><path d="M9.5 4.5A3 3 0 0 1 15 6v4.5"/><path d="M9 9.5V12a3 3 0 0 0 4.2 2.75"/><path d="M5 11a7 7 0 0 0 10.4 6.1"/><path d="M19 11a7 7 0 0 1-1.2 3.9"/><path d="M12 18v3M9 21h6M4 4l16 16"/></svg>`, headphonesOff: `<svg viewBox="0 0 24 24"><path d="M4 14v-2a8 8 0 0 1 12.4-6.7"/><path d="M20 14v-2c0-1.1-.2-2.1-.6-3"/><path d="M4 14h4v6H6a2 2 0 0 1-2-2z"/><path d="M17 14h3v4"/><path d="M4 4l16 16"/></svg>`, settings: `<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19 12a7 7 0 0 0-.1-1.1l2-1.5-2-3.4-2.4 1a7 7 0 0 0-1.9-1.1L14.3 3h-4.6l-.4 2.9A7 7 0 0 0 7.5 7L5.1 6l-2 3.4 2 1.5A7 7 0 0 0 5 12a7 7 0 0 0 .1 1.1l-2 1.5 2 3.4 2.4-1a7 7 0 0 0 1.9 1.1l.4 2.9h4.6l.4-2.9a7 7 0 0 0 1.9-1.1l2.4 1 2-3.4-2-1.5A7 7 0 0 0 19 12z"/></svg>`, keyboard: `<svg viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M7 9h.01M11 9h.01M15 9h.01M7 13h.01M11 13h.01M15 13h.01M8 17h8"/></svg>`, arrowUp: `<svg viewBox="0 0 24 24"><path d="M12 19V5M6 11l6-6 6 6"/></svg>`, back: `<svg viewBox="0 0 24 24"><path d="M19 12H5"/><path d="m12 19-7-7 7-7"/></svg>`, speaker: `<svg viewBox="0 0 24 24"><path d="M4 10v4h4l5 4V6L8 10z"/><path d="M16 9a5 5 0 0 1 0 6M19 7a9 9 0 0 1 0 10"/></svg>`, sliders: `<svg viewBox="0 0 24 24"><path d="M4 6h10M18 6h2M4 12h3M11 12h9M4 18h12M20 18h0"/><circle cx="16" cy="6" r="2"/><circle cx="9" cy="12" r="2"/><circle cx="18" cy="18" r="2"/></svg>`, translate: `<svg viewBox="0 0 24 24"><path d="M4 5h9M9 5v14M5 9c1.2 3.8 4.1 6.4 8 8"/><path d="M14 19l4-9 4 9M15.5 16h5"/></svg>`, code: `<svg viewBox="0 0 24 24"><path d="M8 8l-4 4 4 4M16 8l4 4-4 4M14 4l-4 16"/></svg>`, monitor: `<svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="13" rx="2"/><path d="M8 21h8M12 17v4"/></svg>`, pulse: `<svg viewBox="0 0 24 24"><path d="M3 12h4l2-6 4 12 2-6h6"/></svg>`, check: `<svg viewBox="0 0 24 24"><path d="M20 6 9 17l-5-5"/></svg>`, logs: `<svg viewBox="0 0 24 24"><path d="M6 3h12v18H6z"/><path d="M9 8h6M9 12h6M9 16h4"/></svg>`, maximize: `<svg viewBox="0 0 24 24"><path d="M8 3H3v5M16 3h5v5M8 21H3v-5M16 21h5v-5"/><path d="M3 3l7 7M21 3l-7 7M3 21l7-7M21 21l-7-7"/></svg>` };
function icon(name: keyof typeof icons): string { return `<span class="icon" aria-hidden="true">${icons[name]}</span>`; }

app.innerHTML = `<section id="warmupScreen" class="warmup-screen"><div class="warmup-card"><div class="warmup-brand"><div class="brand-orb">T</div><div><p>TRANSLATEIT</p><h1>Preparing local voice translation</h1></div></div><p id="warmupDetail" class="warmup-detail">Starting desktop shell...</p><div class="warmup-meter"><div id="warmupFill" class="warmup-fill"></div></div><div class="warmup-meta"><span id="warmupPercent">0%</span><span>Release-first startup</span></div><ol id="warmupSteps" class="warmup-steps"></ol></div></section><main id="mainApp" class="app-shell is-hidden"><aside class="sidebar"><section class="brand-row"><div class="brand-mark">T</div><div><h1>TRANSLATEIT</h1><p>Local voice translation</p></div></section><button id="newChatButton" class="new-chat-button" type="button">${icon("plus")}<span>New Chat</span></button><nav class="nav-stack"><p class="nav-heading">Recent Chat</p><button id="recentChatButton" class="nav-item" type="button">${icon("clock")}<span>Recent Chat</span>${icon("chevron")}</button><button id="unsavedChatButton" class="nav-item" type="button">${icon("file")}<span>Unsaved Chat</span>${icon("chevron")}</button><div class="nav-divider"></div><p class="nav-heading">Workspace</p><button id="savedChatButton" class="nav-item" type="button">${icon("folder")}<span>Saved Chat</span>${icon("chevron")}</button><button id="localDataButton" class="nav-item" type="button">${icon("shield")}<span>Local Data</span>${icon("chevron")}</button></nav><section class="account-card"><div class="avatar">HK</div><div class="account-text"><strong>Marcel Berc...</strong><span id="userPresence">Invisible</span></div><div class="account-actions"><button id="quickMicButton" class="footer-icon danger" type="button">${icon("micOff")}</button><button id="micOptionsButton" class="footer-dropdown danger" type="button">${icon("chevron")}</button><button id="voiceOutputButton" class="footer-icon danger" type="button">${icon("headphonesOff")}</button><button id="voiceOptionsButton" class="footer-dropdown danger" type="button">${icon("chevron")}</button><button id="settingsButton" class="footer-icon settings-action" type="button">${icon("settings")}</button></div></section></aside><section id="homePage" class="workspace"><header class="topbar"><div><h2>Voice translation</h2><p>Speak Indonesian. Get translated English voice output.</p></div><div class="top-actions"><span id="directionPill" class="direction-pill">ID &gt; EN</span><button id="recordStatusButton" class="record-pill" type="button"><span></span><strong id="recordStatusText">Ready</strong></button></div></header><section class="hero-panel"><span class="hero-kicker">Local-first voice translation</span><h3 id="heroTitle">How can I help translate today?</h3><p id="heroSubtitle">Type a message, or press the microphone button on the right to record speech locally.</p><div id="chatList" class="feature-grid"><article class="feature-card"><div class="feature-title-row"><div class="feature-icon">${icon("keyboard")}</div><h4>Text input</h4></div><p>Type or paste Indonesian text and get an English translation in the conversation.</p></article><article class="feature-card"><div class="feature-title-row"><div class="feature-icon">${icon("mic")}</div><h4>Voice input</h4></div><p>Press the microphone button. A recording indicator appears while voice capture is active.</p></article></div><article class="assistant-card"><div class="mini-brand">T</div><div><strong>TranslateIT</strong><p id="assistantMessage">Startup warmup completed. Local runtime status is being checked.</p></div></article></section><section class="composer-wrap"><div class="composer"><button id="composerPlusButton" class="composer-icon" type="button">${icon("plus")}</button><input id="messageInput" type="text" placeholder="Ask anything..." autocomplete="off" /><button id="microphoneButton" class="composer-icon emphasis" type="button">${icon("mic")}</button><button id="sendButton" class="send-button" type="button">${icon("arrowUp")}</button></div><p class="composer-help">Type a message, or press the microphone button on the right to record speech locally.</p></section></section><section id="settingsPage" class="settings-page is-hidden"><aside class="settings-sidebar"><h2>Settings</h2><nav class="settings-nav-v22"><button class="settings-nav-item" data-settings-tab="general" type="button">${icon("sliders")}<span>General</span></button><button class="settings-nav-item" data-settings-tab="audio" type="button">${icon("speaker")}<span>Audio</span></button><button class="settings-nav-item" data-settings-tab="translate" type="button">${icon("translate")}<span>Translate</span></button><button class="settings-nav-item active" data-settings-tab="developer" type="button">${icon("code")}<span>Developer</span></button></nav></aside><section class="settings-workspace-v22"><header class="settings-topbar-v22"><button id="backHomeButton" class="settings-back-button" type="button">${icon("back")}<span>Back</span></button></header><div id="settingsContent" class="settings-scroll-v22" style="padding-top:72px;"></div></section></section><div class="runtime-sinks" aria-hidden="true"><span id="realtimeStatus">Checking</span><span id="qualityStatus">Checking</span><span id="gpuStatus">Checking</span><pre id="developerOutput">Runtime status will appear here after warmup.</pre></div></main>`;

function requireElement<T extends Element>(selector: string): T { const element = document.querySelector<T>(selector); if (!element) throw new Error(`${selector} was not found.`); return element; }
const ui = { warmupScreen: requireElement<HTMLElement>("#warmupScreen"), warmupFill: requireElement<HTMLDivElement>("#warmupFill"), warmupPercent: requireElement<HTMLSpanElement>("#warmupPercent"), warmupDetail: requireElement<HTMLParagraphElement>("#warmupDetail"), warmupSteps: requireElement<HTMLOListElement>("#warmupSteps"), mainApp: requireElement<HTMLElement>("#mainApp"), homePage: requireElement<HTMLElement>("#homePage"), settingsPage: requireElement<HTMLElement>("#settingsPage"), settingsContent: requireElement<HTMLElement>("#settingsContent"), settingsButton: requireElement<HTMLButtonElement>("#settingsButton"), backHomeButton: requireElement<HTMLButtonElement>("#backHomeButton"), messageInput: requireElement<HTMLInputElement>("#messageInput"), sendButton: requireElement<HTMLButtonElement>("#sendButton"), microphoneButton: requireElement<HTMLButtonElement>("#microphoneButton"), quickMicButton: requireElement<HTMLButtonElement>("#quickMicButton"), recordStatusButton: requireElement<HTMLButtonElement>("#recordStatusButton"), recordStatusText: requireElement<HTMLElement>("#recordStatusText"), assistantMessage: requireElement<HTMLParagraphElement>("#assistantMessage"), heroTitle: requireElement<HTMLHeadingElement>("#heroTitle"), heroSubtitle: requireElement<HTMLParagraphElement>("#heroSubtitle"), realtimeStatus: requireElement<HTMLSpanElement>("#realtimeStatus"), qualityStatus: requireElement<HTMLSpanElement>("#qualityStatus"), gpuStatus: requireElement<HTMLSpanElement>("#gpuStatus"), developerOutput: requireElement<HTMLPreElement>("#developerOutput"), userPresence: requireElement<HTMLSpanElement>("#userPresence"), newChatButton: requireElement<HTMLButtonElement>("#newChatButton"), composerPlusButton: requireElement<HTMLButtonElement>("#composerPlusButton"), recentChatButton: requireElement<HTMLButtonElement>("#recentChatButton"), unsavedChatButton: requireElement<HTMLButtonElement>("#unsavedChatButton"), savedChatButton: requireElement<HTMLButtonElement>("#savedChatButton"), localDataButton: requireElement<HTMLButtonElement>("#localDataButton"), micOptionsButton: requireElement<HTMLButtonElement>("#micOptionsButton"), voiceOutputButton: requireElement<HTMLButtonElement>("#voiceOutputButton"), voiceOptionsButton: requireElement<HTMLButtonElement>("#voiceOptionsButton"), chatList: requireElement<HTMLElement>("#chatList"), settingsNavItems: Array.from(document.querySelectorAll<HTMLButtonElement>("[data-settings-tab]")), navItems: Array.from(document.querySelectorAll<HTMLButtonElement>(".nav-item")), directionPill: requireElement<HTMLElement>("#directionPill") };
let latestBundle: RuntimeStatusBundleReport | null = null; let latestDiagnostics: RuntimeDiagnostics | null = null; let latestHardware: HardwareUsageReport | null = null; let currentSettings: RuntimeSettings | null = null; let activeSettingsTab: SettingsTab = "developer"; let recording = false; let currentSessionId: string | null = null; let activeSessionTitle = "New Chat"; let logsExpanded = false;
async function call<T>(command: string, args?: Record<string, unknown>): Promise<T | null> { try { return args ? await invoke<T>(command, args) : await invoke<T>(command); } catch (error) { console.warn(`TranslateIT command failed: ${command}`, error); return null; } }
function defaultSettings(): RuntimeSettings { return { schema_version: 3, language_focus_mode: "id-en-focus", runtime_profile: "Realtime", source_language: "id", target_language: "en", audio: { input_device_id: null, output_device_id: null, sensitivity: 1, input_sensitivity: "Realtime", show_advanced_devices: false, allow_low_but_usable_input: true, allow_cpu_degraded_mode: false, auto_play_translation_voice: true, auto_play_out_voice: true, use_custom_voice_actor: true, voice_actor_profiles_root: "EngineData/VoiceActorProfiles" }, voice_actor_profile_id: "marcel" }; }
function errorMessage(error: unknown): string { return error instanceof Error ? error.message : String(error); }
function updateWarmup(progress: number, detail: string): void { ui.warmupFill.style.width = `${progress}%`; ui.warmupPercent.textContent = `${progress}%`; ui.warmupDetail.textContent = detail; }
function setAssistantNotice(message: string): void { ui.assistantMessage.textContent = message; }
function setActiveNav(activeButton: HTMLButtonElement | null): void { ui.navItems.forEach((button) => button.classList.toggle("active", button === activeButton)); }
function workerManifest(bundle: RuntimeStatusBundleReport | null) { return bundle?.local_worker_manifest ?? bundle?.internal_validation_gate?.local_worker_manifest ?? null; }
function modelReadyText(value: boolean): string { return value ? "Ready" : "Needs setup"; }
function setRecordingState(active: boolean): void { recording = active; document.body.classList.toggle("is-recording", active); ui.recordStatusText.textContent = active ? "Recording" : "Ready"; ui.microphoneButton.setAttribute("aria-label", active ? "Stop recording" : "Start voice recording"); }
function languageName(code: string): string { if (code.toLowerCase().startsWith("id")) return "Indonesian"; if (code.toLowerCase().startsWith("en")) return "English"; return code.toUpperCase(); }
function refreshDirectionPill(): void { const settings = currentSettings ?? defaultSettings(); ui.directionPill.textContent = `${settings.source_language.toUpperCase()} > ${settings.target_language.toUpperCase()}`; }
function percentText(metric?: HardwareMetric): string { return typeof metric?.percent === "number" ? `${Math.round(metric.percent)}%` : "N/A"; }
function renderWarmupSteps(activeIndex = -1): void { const steps = ["Desktop shell", "User settings", "Audio devices", "GPU policy", "Model assets", "Local AI worker", "Interface"]; ui.warmupSteps.innerHTML = steps.map((title, index) => `<li class="warmup-step ${index < activeIndex ? "complete" : index === activeIndex ? "active" : "pending"}"><span></span><div><strong>${title}</strong><p>${index === activeIndex ? "Checking..." : "Ready for startup check."}</p></div></li>`).join(""); }
function renderRuntime(bundle: RuntimeStatusBundleReport | null, diagnostics: RuntimeDiagnostics | null): void { latestBundle = bundle; latestDiagnostics = diagnostics; setRecordingState(Boolean(bundle?.live_capture.stream_active)); if (!bundle) { ui.userPresence.textContent = "Checking"; setAssistantNotice("Runtime status is not available yet. Open Developer settings for diagnostics."); return; } const worker = workerManifest(bundle); const allModelsReady = Boolean(worker?.asr_model_ready && worker.realtime_translation_model_ready && worker.quality_translation_model_ready && worker.piper_ready); const appReady = Boolean(worker?.ok || bundle.readiness.ready_for_user_facing_runtime || allModelsReady); const blockers = [...bundle.readiness.blockers, ...bundle.capture_gate.blockers, ...(worker?.blockers ?? []), ...(bundle.internal_validation_gate?.blockers ?? [])].filter(Boolean); ui.userPresence.textContent = appReady ? "Ready" : "Setup needed"; ui.heroTitle.textContent = recording ? "Listening locally..." : "How can I help translate today?"; ui.heroSubtitle.textContent = recording ? "Speak now. The local capture runtime is active." : "Type a message, or press the microphone button on the right to record speech locally."; ui.realtimeStatus.textContent = worker ? modelReadyText(worker.asr_model_ready && worker.realtime_translation_model_ready && worker.piper_ready) : "Checking"; ui.qualityStatus.textContent = worker ? modelReadyText(worker.asr_model_ready && worker.quality_translation_model_ready && worker.piper_ready) : "Checking"; ui.gpuStatus.textContent = diagnostics?.cuda_probe.cuda_runtime_ready ? "CUDA ready" : diagnostics?.cuda_probe.gpu_summary ? "GPU detected" : "CPU fallback"; ui.developerOutput.textContent = JSON.stringify({ app_version: bundle.engine_status.app_version, lifecycle: bundle.engine_status.lifecycle_state, hardware: latestHardware, local_worker: worker, recording_active: recording, cuda: diagnostics?.cuda_probe, next_action: bundle.next_action, blockers: blockers.slice(0, 12) }, null, 2); if (!currentSessionId) setAssistantNotice(appReady ? "Local runtime warmup completed. You can start typing or record speech." : `Warmup completed, but setup is not fully ready yet. ${blockers[0] ? blockers[0].replaceAll("_", " ") : bundle.next_action}`); }
function showHome(): void { document.body.classList.remove("settings-open"); ui.settingsPage.classList.add("is-hidden"); ui.homePage.classList.remove("is-hidden"); }
function showSettings(): void { document.body.classList.add("settings-open"); ui.homePage.classList.add("is-hidden"); ui.settingsPage.classList.remove("is-hidden"); renderSettingsTab(activeSettingsTab); }
async function refreshHardwareUsage(): Promise<void> { latestHardware = await call<HardwareUsageReport>("get_hardware_usage"); }
async function ensureChatSession(): Promise<string | null> { if (currentSessionId) return currentSessionId; const session = await call<LauncherChatSession>("create_chat_session", { kind: "unsaved" }); currentSessionId = session?.session_id ?? null; activeSessionTitle = session?.title ?? "New Chat"; return currentSessionId; }
async function createNewChat(): Promise<void> { const session = await call<LauncherChatSession>("create_chat_session", { kind: "unsaved" }); currentSessionId = session?.session_id ?? null; activeSessionTitle = session?.title ?? "New Chat"; ui.messageInput.value = ""; setActiveNav(null); renderHomeCards(); showHome(); setAssistantNotice(currentSessionId ? "New chat saved locally and ready." : "New chat is ready, but backend session creation failed."); }
async function saveChatMessage(role: "user" | "assistant", content: string): Promise<void> { const sessionId = await ensureChatSession(); if (!sessionId) return; const result = await call<LauncherChatActionResult>("append_chat_message", { sessionId, role, content }); if (result?.ok && role === "user" && activeSessionTitle === "New Chat") activeSessionTitle = content.split(/\s+/).slice(0, 8).join(" "); }
async function showChatCollection(kind: ChatKind, button: HTMLButtonElement): Promise<void> { setActiveNav(button); showHome(); const listKind = kind === "local" ? undefined : kind; const sessions = await call<LauncherChatSummary[]>("list_chat_sessions", listKind ? { kind: listKind } : {}); const rows = sessions ?? []; ui.chatList.innerHTML = rows.length ? rows.slice(0, 6).map((item) => `<article class="feature-card"><div class="feature-title-row"><div class="feature-icon">${icon(kind === "saved" ? "folder" : "file")}</div><h4>${item.title}</h4></div><p>${item.kind} · ${item.message_count} message(s)</p></article>`).join("") : `<article class="feature-card"><div class="feature-title-row"><div class="feature-icon">${icon("file")}</div><h4>No ${kind} chat yet</h4></div><p>New chat sessions will appear here after you send a message.</p></article>`; setAssistantNotice(`${kind === "local" ? "Local Data" : kind} opened. ${rows.length} item(s) found.`); }
function renderHomeCards(): void { ui.chatList.innerHTML = `<article class="feature-card"><div class="feature-title-row"><div class="feature-icon">${icon("keyboard")}</div><h4>Text input</h4></div><p>Type or paste Indonesian text and get an English translation in the conversation.</p></article><article class="feature-card"><div class="feature-title-row"><div class="feature-icon">${icon("mic")}</div><h4>Voice input</h4></div><p>Press the microphone button. A recording indicator appears while voice capture is active.</p></article>`; }
async function submitText(): Promise<void> { const source = ui.messageInput.value.trim(); if (!source) return; ui.messageInput.value = ""; await saveChatMessage("user", source); setAssistantNotice("Translating text locally..."); const result = await call<CommandResult>("translate_text", { source }); const response = result?.message ?? "Translation command failed. Open Settings > Developer for diagnostics."; await saveChatMessage("assistant", response); setAssistantNotice(response); }
async function startOrStopRecording(): Promise<void> { const result = recording ? await call<CommandResult>("stop_capture") : await call<CommandResult>("start_capture"); setAssistantNotice(result?.message ?? (recording ? "Recording stopped." : "Recording started. Waiting for local capture status.")); const bundle = await call<RuntimeStatusBundleReport>("get_runtime_status_bundle"); renderRuntime(bundle, latestDiagnostics); }
async function checkAudioInput(): Promise<void> { const status = await call<InputPreparationStatus>("get_input_status"); const label = document.getElementById("audioInputLabel"); if (label) label.textContent = status?.selected_device_name ?? "Default microphone"; setAssistantNotice(status?.note ?? status?.blocker ?? "Audio input status checked."); }
async function saveCurrentSettings(): Promise<void> { const result = await call<CommandResult>("save_runtime_settings", { settings: currentSettings ?? defaultSettings() }); setAssistantNotice(result?.message ?? "Save settings command failed."); }
async function saveDefaultSettings(): Promise<void> { const result = await call<CommandResult>("save_default_runtime_settings"); currentSettings = await call<RuntimeSettings>("load_runtime_settings") ?? currentSettings; refreshDirectionPill(); setAssistantNotice(result?.message ?? "Default settings save command failed."); }
async function runDeveloperDiagnostic(): Promise<void> { const [bundle, diagnostics] = await Promise.all([call<RuntimeStatusBundleReport>("get_runtime_status_bundle"), call<RuntimeDiagnostics>("get_runtime_diagnostics")]); await refreshHardwareUsage(); renderRuntime(bundle, diagnostics); renderDeveloperSettings(); }
function openAudioSettings(): void { activeSettingsTab = "audio"; showSettings(); }
function toggleVoiceOutput(): void { currentSettings = currentSettings ?? defaultSettings(); currentSettings.audio.auto_play_out_voice = !currentSettings.audio.auto_play_out_voice; currentSettings.audio.auto_play_translation_voice = currentSettings.audio.auto_play_out_voice; setAssistantNotice(currentSettings.audio.auto_play_out_voice ? "Voice output enabled." : "Voice output disabled."); }
function setRuntimeProfile(profile: "Realtime" | "Quality"): void { currentSettings = currentSettings ?? defaultSettings(); currentSettings.runtime_profile = profile; currentSettings.audio.input_sensitivity = profile; setAssistantNotice(`Translate mode set to ${profile}.`); }
function swapLanguages(): void { currentSettings = currentSettings ?? defaultSettings(); const source = currentSettings.source_language; currentSettings.source_language = currentSettings.target_language; currentSettings.target_language = source; refreshDirectionPill(); setAssistantNotice(`Language pair changed to ${currentSettings.source_language.toUpperCase()} > ${currentSettings.target_language.toUpperCase()}.`); }
function renderSettingsTab(tab: SettingsTab): void { activeSettingsTab = tab; ui.settingsNavItems.forEach((button) => button.classList.toggle("active", button.dataset.settingsTab === tab)); if (tab === "general") renderGeneralSettings(); if (tab === "audio") renderAudioSettings(); if (tab === "translate") renderTranslateSettings(); if (tab === "developer") renderDeveloperSettings(); }
function renderGeneralSettings(): void { const settings = currentSettings ?? defaultSettings(); ui.settingsContent.innerHTML = `<section class="settings-page-title"><h2>General</h2><p>Basic launcher and local runtime preferences.</p></section><article class="audio-card-v22"><div class="audio-grid-v22"><section class="audio-field-group"><h3>Runtime Profile</h3><button class="select-field-v22" type="button"><span>${settings.runtime_profile}</span>${icon("chevron")}</button></section><section class="audio-field-group"><h3>Language Focus</h3><button class="select-field-v22" type="button"><span>${settings.language_focus_mode}</span>${icon("chevron")}</button></section><section class="audio-field-group"><h3>Realtime Status</h3><button class="select-field-v22" type="button"><span>${ui.realtimeStatus.textContent}</span>${icon("pulse")}</button></section><section class="audio-field-group"><h3>GPU Status</h3><button class="select-field-v22" type="button"><span>${ui.gpuStatus.textContent}</span>${icon("monitor")}</button></section></div><button id="saveSettingsButton" class="mic-test-button-v22" type="button">Save Settings</button><button id="resetSettingsButton" class="mic-test-button-v22" type="button" style="margin-left:12px;">Save Default</button></article><section class="settings-page-title secondary"><h2>Advanced General Setting</h2><p>Reserved for future launcher preferences.</p></section><article class="advanced-empty-v22"></article>`; requireElement<HTMLButtonElement>("#saveSettingsButton").addEventListener("click", () => void saveCurrentSettings()); requireElement<HTMLButtonElement>("#resetSettingsButton").addEventListener("click", () => void saveDefaultSettings()); }
function renderAudioSettings(): void { const settings = currentSettings ?? defaultSettings(); const voiceEnabled = settings.audio.auto_play_out_voice; ui.settingsContent.innerHTML = `<section class="settings-page-title"><h2>Audio</h2><p>Configure microphone input, voice output, and local capture checks.</p></section><article class="audio-card-v22"><div class="audio-grid-v22"><section class="audio-field-group"><h3>Input Device</h3><button id="checkAudioInputButton" class="select-field-v22" type="button"><span id="audioInputLabel">Default microphone</span>${icon("chevron")}</button></section><section class="audio-field-group"><h3>Input Sensitivity</h3><button id="audioSensitivityButton" class="select-field-v22" type="button"><span>${settings.audio.input_sensitivity}</span>${icon("chevron")}</button></section><section class="audio-field-group"><h3>Voice Output</h3><button id="audioVoiceToggleButton" class="select-field-v22" type="button"><span>${voiceEnabled ? "Enabled" : "Disabled"}</span>${icon("speaker")}</button></section><section class="mic-test-row-v22"><button id="micTestButton" class="mic-test-button-v22" type="button">Test Mic</button><div class="meter-v22"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></div></section></div></article><section class="settings-page-title secondary"><h2>Advanced Audio Setting</h2><p>Reserved for future audio device options.</p></section><article class="advanced-empty-v22"></article>`; requireElement<HTMLButtonElement>("#checkAudioInputButton").addEventListener("click", () => void checkAudioInput()); requireElement<HTMLButtonElement>("#micTestButton").addEventListener("click", () => void startOrStopRecording()); requireElement<HTMLButtonElement>("#audioVoiceToggleButton").addEventListener("click", () => { toggleVoiceOutput(); renderAudioSettings(); }); requireElement<HTMLButtonElement>("#audioSensitivityButton").addEventListener("click", () => { setRuntimeProfile((currentSettings ?? defaultSettings()).runtime_profile === "Quality" ? "Realtime" : "Quality"); renderAudioSettings(); }); }
function renderTranslateSettings(): void { const settings = currentSettings ?? defaultSettings(); const realtimeActive = settings.runtime_profile !== "Quality"; ui.settingsContent.innerHTML = `<section class="settings-page-title"><h2>Translate</h2><p>Configure language pair, realtime mode, and output style.</p></section><article class="audio-card-v22"><div class="audio-grid-v22"><section class="audio-field-group"><h3>Source Language</h3><button class="select-field-v22" type="button"><span>${languageName(settings.source_language)}</span>${icon("translate")}</button></section><section class="audio-field-group"><h3>Target Language</h3><button class="select-field-v22" type="button"><span>${languageName(settings.target_language)}</span>${icon("translate")}</button></section></div><button id="swapLanguageButton" class="mic-test-button-v22" type="button">Swap</button><button id="saveTranslateButton" class="mic-test-button-v22" type="button" style="margin-left:12px;">Save</button></article><section class="settings-page-title secondary"><h2>Realtime</h2><p>Choose faster response or higher quality translation planning.</p></section><article class="voice-card-v22"><div class="voice-grid-v22"><section><h3>Mode</h3><div id="realtimeModeButton" class="radio-row-v22 ${realtimeActive ? "active" : ""}" role="button"><span></span><strong>Fast</strong><em>Prioritize realtime latency.</em></div><div id="qualityModeButton" class="radio-row-v22 ${!realtimeActive ? "active" : ""}" role="button"><span></span><strong>Quality</strong><em>Prioritize careful translation planning.</em></div></section><section><h3>Translate Output</h3><div class="radio-row-v22 active"><span></span><strong>Transcript</strong><em>Always show translated text.</em></div><div class="radio-row-v22 ${settings.audio.auto_play_out_voice ? "active" : ""}"><span></span><strong>Voice</strong><em>Play translated voice when local TTS is available.</em></div></section></div></article><section class="settings-page-title secondary"><h2>Advanced Translate Setting</h2><p>Reserved for future translation preferences.</p></section><article class="advanced-empty-v22"></article>`; requireElement<HTMLButtonElement>("#swapLanguageButton").addEventListener("click", () => { swapLanguages(); renderTranslateSettings(); }); requireElement<HTMLButtonElement>("#saveTranslateButton").addEventListener("click", () => void saveCurrentSettings()); requireElement<HTMLElement>("#realtimeModeButton").addEventListener("click", () => { setRuntimeProfile("Realtime"); renderTranslateSettings(); }); requireElement<HTMLElement>("#qualityModeButton").addEventListener("click", () => { setRuntimeProfile("Quality"); renderTranslateSettings(); }); }
function renderDeveloperSettings(): void { const progress = latestBundle?.internal_validation_gate?.progress_percent ?? latestBundle?.live_pipeline_gate?.progress_percent ?? 0; const cpu = percentText(latestHardware?.cpu); const ram = percentText(latestHardware?.ram); const gpu = percentText(latestHardware?.gpu); const gpuStatus = latestDiagnostics?.cuda_probe.gpu_summary ?? latestHardware?.gpu.detail ?? "GPU status unavailable"; const logRows = [`<p><strong>[OK]</strong>${latestBundle ? "Runtime status loaded." : "Waiting for diagnostic check."}</p>`, `<p><strong>[HW]</strong>CPU ${cpu} · RAM ${ram} · GPU ${gpu}</p>`, `<p><strong>[GPU]</strong>${gpuStatus}</p>`, `<p><strong>[WAIT]</strong>${latestBundle?.next_action ?? "Waiting for next diagnostic result."}</p>`].join(""); ui.settingsContent.innerHTML = `<section class="settings-page-title"><h2>Developer</h2><p>Simple tools for monitoring runtime health and fixing common issues.</p></section><section class="settings-page-title secondary"><h2>Monitoring</h2><p>Melacak usage hardware dan health engine.</p></section><article class="audio-card-v22"><div class="audio-grid-v22"><section class="audio-field-group"><h3>CPU Usage</h3><button class="select-field-v22" type="button"><span>${cpu}</span>${icon("monitor")}</button></section><section class="audio-field-group"><h3>RAM Usage</h3><button class="select-field-v22" type="button"><span>${ram}</span>${icon("monitor")}</button></section><section class="audio-field-group"><h3>GPU Usage</h3><button class="select-field-v22" type="button"><span>${gpu}</span>${icon("monitor")}</button></section><section class="audio-field-group"><h3>Health Engine</h3><button class="select-field-v22" type="button"><span>${latestBundle ? "Good" : "Check"}</span>${icon("pulse")}</button></section></div><p style="margin:26px 0 0;color:var(--muted);font-size:13px;">${latestHardware?.note ?? "Run diagnostic to refresh hardware usage."}</p></article><section class="settings-page-title secondary"><h2>Diagnostic</h2><p>Run checking and review diagnostic logs.</p></section><article class="audio-card-v22"><button id="runDiagnosticButton" class="mic-test-button-v22" type="button">Run Checking</button><span style="margin-left:16px;color:var(--muted);font-weight:800;">${Math.round(progress)}%</span><section id="diagnosticLogPanel" class="developer-log-body" style="height:${logsExpanded ? 320 : 176}px;margin-top:28px;border:1px solid var(--border-strong);border-radius:18px;background:#080b11;padding:26px;overflow:hidden;">${logRows}</section><button id="seeAllLogsButton" class="mic-test-button-v22" type="button" style="margin-top:18px;">${logsExpanded ? "Show Less" : "See All Logs"}</button></article><section class="settings-page-title secondary"><h2>Advanced Developer Setting</h2><p>Reserved for future developer options.</p></section><article class="advanced-empty-v22"></article>`; requireElement<HTMLButtonElement>("#runDiagnosticButton").addEventListener("click", () => void runDeveloperDiagnostic()); requireElement<HTMLButtonElement>("#seeAllLogsButton").addEventListener("click", () => { logsExpanded = !logsExpanded; renderDeveloperSettings(); }); }
async function runWarmup(): Promise<void> { const steps = [12, 24, 38, 52, 68, 84, 100]; for (let i = 0; i < steps.length; i += 1) { renderWarmupSteps(i); updateWarmup(steps[i], "Checking local runtime..."); await new Promise((resolve) => window.setTimeout(resolve, 120)); } currentSettings = await call<RuntimeSettings>("load_runtime_settings") ?? defaultSettings(); refreshDirectionPill(); await refreshHardwareUsage(); const [bundle, diagnostics] = await Promise.all([call<RuntimeStatusBundleReport>("get_runtime_status_bundle"), call<RuntimeDiagnostics>("get_runtime_diagnostics")]); renderRuntime(bundle, diagnostics); renderSettingsTab("developer"); ui.warmupScreen.classList.add("is-hidden"); ui.mainApp.classList.remove("is-hidden"); }
ui.settingsButton.addEventListener("click", showSettings); ui.backHomeButton.addEventListener("click", showHome); ui.microphoneButton.addEventListener("click", () => void startOrStopRecording()); ui.quickMicButton.addEventListener("click", () => void startOrStopRecording()); ui.recordStatusButton.addEventListener("click", () => void startOrStopRecording()); ui.sendButton.addEventListener("click", () => void submitText()); ui.messageInput.addEventListener("keydown", (event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); void submitText(); } }); ui.newChatButton.addEventListener("click", () => void createNewChat()); ui.composerPlusButton.addEventListener("click", () => { ui.messageInput.focus(); setAssistantNotice("Input is ready. File attachment backend is not connected yet."); }); ui.recentChatButton.addEventListener("click", () => void showChatCollection("recent", ui.recentChatButton)); ui.unsavedChatButton.addEventListener("click", () => void showChatCollection("unsaved", ui.unsavedChatButton)); ui.savedChatButton.addEventListener("click", () => void showChatCollection("saved", ui.savedChatButton)); ui.localDataButton.addEventListener("click", () => void showChatCollection("local", ui.localDataButton)); ui.micOptionsButton.addEventListener("click", openAudioSettings); ui.voiceOutputButton.addEventListener("click", () => { toggleVoiceOutput(); }); ui.voiceOptionsButton.addEventListener("click", openAudioSettings); ui.settingsNavItems.forEach((button) => button.addEventListener("click", () => renderSettingsTab(button.dataset.settingsTab as SettingsTab)));
void runWarmup().catch((error: unknown) => { updateWarmup(100, `Warmup finished with warning: ${errorMessage(error)}`); ui.warmupScreen.classList.add("is-hidden"); ui.mainApp.classList.remove("is-hidden"); });
